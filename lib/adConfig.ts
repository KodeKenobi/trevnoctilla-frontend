// Propeller Ads Configuration
export const AD_ZONE_ID = "YOUR_ZONE_ID_HERE"; // Replace after signup with real zone ID
export const USE_FAKE_AD_FALLBACK = true; // Falls back to fake ad if real ad fails

// Propeller Ads base URL
export const PROPELLER_ADS_URL = "//propellerads.com/js/adsbypropeller.js";

// Ad loading timeout (in milliseconds)
export const AD_LOAD_TIMEOUT = 10000; // 10 seconds
